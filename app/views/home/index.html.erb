<div class="wrapper" ng-app="edzoApp">
  <div class="controls">
    <input ng-model="test" type="text">
    {{ test }}
  </div>
  <div class="main">
    <div class="graph" ng-controller="GraphCtrl">
      <node ng-repeat="person in people">i</node>
    </div>
  </div>
</div>
